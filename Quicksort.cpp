#include<iostream>
using namespace std;
int array [5] = {5,8,4,12,5}, n=5;

int partition (int low , int high)
{
    int pivot = array[high];
    int i = (low-1);
    for (int j low)
}
 void quicksort (int low , int high)
 {
    if (low < high)
    {

    }
 }